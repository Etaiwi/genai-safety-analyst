# GenAI Safety Analyst Environment Configuration

# === Required API Keys ===
# Groq API key for LLM calls
GROQ_API_KEY=your-groq-api-key-here

# Hugging Face token for downloading sentence-transformer models reliably
HF_TOKEN=your-huggingface-token-here

# === Security Guardrails Configuration ===
# Maximum text length allowed per request (characters)
MAX_TEXT_CHARS=1200

# Rate limiting: Maximum requests per IP address
RATE_LIMIT_MAX_REQUESTS=20

# Rate limiting: Time window in seconds for rate limiting
RATE_LIMIT_WINDOW_SECONDS=60

# === Optional Features ===
# Enable demo token authentication for the public UI.
# If set, clients must send `X-Demo-Token: <value>` in the request headers.
# Leave commented out to disable this check.
# DEMO_TOKEN=your-demo-token-here
